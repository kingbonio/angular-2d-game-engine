import { armour, potions, weapons, keyItems } from '../items';
import { IInventoryItem } from '../../game/item/interfaces';

export const initialInventoryItems: IInventoryItem[] = [
    armour.leatherChestPiece,
];
