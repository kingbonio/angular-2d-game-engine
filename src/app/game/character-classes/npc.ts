import { Character } from "./character";

export class NPC extends Character {
      private element: any;

      constructor(element) {
            super();
            this.element = element;
      }

}
